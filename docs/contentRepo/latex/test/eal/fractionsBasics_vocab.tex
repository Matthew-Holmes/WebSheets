% !TeX program = lualatex
% ================================================================
% Tier 3 Vocabulary Key for FractionsBasics
%
% Generated by the server using the following settings:
%
%   built from          latex/test/eal/fractionsBasics.tex
%   tier 3 vocabulary   green   RGB 0 128 0
%   English text        black   RGB 0 0 0
%   translated text     purple  RGB 112 48 160
%   layout macros       version 1
%
% Please don't edit any information in this comment block - the server
% compares it against the current settings and rebuilds this file when
% they differ, so changes here would be overwritten. However, feel free
% to make updates to the LaTeX below if you want to edit it.
% ================================================================
% ==== VOCABULARY DATA - generated, do not edit ====
% {"terms":[{"en":"fraction","def":"a number written as one whole split into equal parts, with a number of those parts taken","tr":"ulamek","trdef":"liczba zapisana jako calosc podzielona na rowne czesci"},
% {"en":"numerator","def":"the number above the line in a fraction, showing how many equal parts are taken","tr":"licznik","trdef":"liczba nad kreska ulamka"},
% {"en":"denominator","def":"the number below the line in a fraction, showing how many equal parts the whole is split into","tr":"mianownik","trdef":"liczba pod kreska ulamka"},
% {"en":"equivalent","def":"having the same value, even though it is written differently","tr":"rownowazny","trdef":"majacy te sama wartosc, choc zapisany inaczej"},
% {"en":"simplify","def":"to write something in its smallest or plainest form without changing its value","tr":"uproscic","trdef":"zapisac w najprostszej postaci bez zmiany wartosci"}]}
% ==== END VOCABULARY DATA ====

\documentclass[11pt]{article}
\usepackage[a4paper,margin=18mm]{geometry}
\usepackage{array}
\newcommand{\ealtext}[1]{#1}
\newcommand{\ealtextblock}[1]{{\raggedright #1\par}}
% ================================================================
% EAL layout helpers
% ================================================================
\usepackage{xcolor}

\definecolor{ealkeycolour}{RGB}{0,128,0}
\definecolor{ealenglish}{RGB}{0,0,0}
\definecolor{ealtwocolour}{RGB}{112,48,160}

\newsavebox{\ealboxen}
\newsavebox{\ealboxtr}
\newlength{\ealglosswd}
\newlength{\ealglossraise}
\setlength{\ealglossraise}{1.15em}

% a tier 3 word, in English and in translation alike
\newcommand{\ealkey}[1]{\textcolor{ealkeycolour}{#1}}
\newcommand{\ealkeytr}[1]{\textcolor{ealkeycolour}{\ealtext{#1}}}

% One translated word above one English word. Built from kernel
% primitives, never a tabular, which would break wherever the sheet
% loads array, booktabs or tabularx. The box is as wide as the wider of
% the two, so a long translation pushes its neighbours aside instead of
% overlapping them, and it claims its full height so a table row or a
% TikZ node makes room for it.
\newcommand{\ealgloss}[2]{%
  \sbox{\ealboxen}{\ealkey{#1}}%
  \sbox{\ealboxtr}{\small\textcolor{ealtwocolour}{\ealtext{#2}}}%
  \setlength{\ealglosswd}{\wd\ealboxen}%
  \ifdim\wd\ealboxtr>\ealglosswd\setlength{\ealglosswd}{\wd\ealboxtr}\fi
  \leavevmode
  \makebox[\ealglosswd][c]{%
    \makebox[0pt][c]{\usebox{\ealboxen}}%
    \makebox[0pt][c]{\raisebox{\ealglossraise}{\usebox{\ealboxtr}}}%
  }%
}

% opens up line spacing around a block of glosses, so the raised
% translations sit clear of the line above
\newenvironment{ealglossed}{\par\linespread{2.1}\selectfont\ignorespaces}{\par}

% a whole translated sentence above its English counterpart. block level,
% so it does not belong inside a tabular cell
\newcommand{\ealpara}[2]{%
  \par\addvspace{0.5\baselineskip}%
  {\color{ealtwocolour}\ealtextblock{#1}}%
  \penalty200
  {\color{ealenglish}#2\par}%
  \addvspace{0.5\baselineskip}%
}

\pagestyle{empty}
\setlength{\parindent}{0pt}

\newcommand{\ealkeybody}{%
  {\large\textbf{FractionsBasics: key vocabulary}}\par\medskip
  \begin{tabular}{@{}p{40mm}p{112mm}@{}}
    \textbf{Word} & \textbf{Meaning} \\[2pt]
    \hline\\[-6pt]
    \ealkey{fraction} & a number written as one whole split into equal parts, with a number of those parts taken \\[4pt]
    \ealkey{numerator} & the number above the line in a fraction, showing how many equal parts are taken \\[4pt]
    \ealkey{denominator} & the number below the line in a fraction, showing how many equal parts the whole is split into \\[4pt]
    \ealkey{equivalent} & having the same value, even though it is written differently \\[4pt]
    \ealkey{simplify} & to write something in its smallest or plainest form without changing its value \\[4pt]
  \end{tabular}}


\newcommand{\ealmatchbody}{%
  {\large\textbf{Match the word to its meaning}}\par\medskip
  \begin{tabular}{@{}p{50mm}@{\hspace{16mm}}p{86mm}@{}}
    \ealkey{fraction} & the number below the line in a fraction, showing how many equal parts the whole is split into \\[10pt]
    \ealkey{numerator} & to write something in its smallest or plainest form without changing its value \\[10pt]
    \ealkey{denominator} & the number above the line in a fraction, showing how many equal parts are taken \\[10pt]
    \ealkey{equivalent} & having the same value, even though it is written differently \\[10pt]
    \ealkey{simplify} & a number written as one whole split into equal parts, with a number of those parts taken \\[10pt]
  \end{tabular}}


\begin{document}
\ealkeybody\newpage\ealkeybody\newpage
\ealmatchbody\newpage\ealmatchbody
\end{document}
